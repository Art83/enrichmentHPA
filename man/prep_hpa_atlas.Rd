% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_hpa_atlas.R
\name{prep_hpa_atlas}
\alias{prep_hpa_atlas}
\title{Prepare HPA-style reference data for enrich_by_region()}
\usage{
prep_hpa_atlas(
  df_ref,
  gene_col = Gene.name,
  group_col = Region,
  expr_col = pTPM,
  drop_all_zero = TRUE
)
}
\arguments{
\item{df_ref}{Data frame in long format with at least:
\itemize{
\item a gene column (default "Gene.name")
\item a grouping column (e.g. "Region", "Immune.cell")
\item an expression column (e.g. "pTPM")
}}

\item{gene_col}{Column containing gene identifiers.}

\item{group_col}{Column containing group labels (regions/cell types).}

\item{expr_col}{Column containing numeric expression values.}

\item{drop_all_zero}{Logical; if TRUE (default), drop genes that are zero
(or NA) across all groups.}
}
\value{
A cleaned data.frame with the same columns as input (plus any extras),
ready to be passed to \code{enrich_by_region()}.
}
\description{
This helper standardises HPA expression data and optionally drops
genes that are zero across all groups.
}
